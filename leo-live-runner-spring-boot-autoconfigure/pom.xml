<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.zlpawn</groupId>
        <artifactId>leo-live-runner</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>leo-live-runner-spring-boot-autoconfigure</artifactId>
    <packaging>jar</packaging>

    <name>leo-live-runner-spring-boot-autoconfigure</name>
    <description>Auto-configuration module for Spring Boot 2.x and 3.x</description>

    <dependencies>
        <!-- Core Engine -->
        <dependency>
            <groupId>io.github.zlpawn</groupId>
            <artifactId>leo-live-runner-core</artifactId>
        </dependency>

        <!-- Spring Boot AutoConfigure (provided by host application) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <version>${spring-boot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Web (provided by host application) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <version>5.3.31</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>5.3.31</version>
            <scope>provided</scope>
        </dependency>

        <!-- Servlet API (provided by host application container) -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>4.0.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- Spring TX & AOP (provided by host application) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <version>5.3.31</version>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aop</artifactId>
            <version>5.3.31</version>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>

        <!-- Configuration Processor for IDE metadata hint -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <version>${spring-boot.version}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
